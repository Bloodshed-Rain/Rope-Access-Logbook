# Exclude locally generated native directories from EAS fingerprint calculation.
# These folders are generated by `expo prebuild` and are listed in .gitignore.
# Their presence or absence on a local machine should not affect the runtime
# version hash, which must match the EAS build environment.
ios/
android/
